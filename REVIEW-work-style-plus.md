# Editorial UI / WORK STYLE + — 変更と確認事項

## この変更の範囲

- 白・ネイビー・ブルーを中心に、ヒーロー、質問カード、結果、16タイプ紹介、プライバシーページの見た目を統一する。
- 既存のキャラクター画像を再利用する。基本20問、出題順、4軸の採点、16タイプ判定、5問単位のページ構成を変えない。
- LPの進捗は常時表示の右下の円だけとする。上流の `afa034b` の変更を取り込んでいる。
- キーボード操作、選択状態の読み上げ、動きを減らす設定に対応する。

## 追加指標

基本20問のあとに、任意の追加9問（3問×3ページ）を設ける。スキップして基本の結果を見ることもできる。

| 指標 | 質問数 | 表示の意味 |
| --- | --- | --- |
| ストレス対処 | 3 | 落ち着き、立て直し、負担調整についての自己評価 |
| プレイヤー志向 | 3 | 自ら手を動かし、専門性を深めたいという役割の好み |
| マネジメント志向 | 3 | 役割の整理や人の成長を支えたいという役割の好み |

5段階の回答1〜5を0〜4点に置き換え、指標ごとに `round(合計 / 12 * 100)` を表示する。3指標は独立している。プレイヤーとマネジメントの合計を100にするものではない。差が17ポイント未満のときは関心が近いと説明する。これは表示用の便宜的な区分で、統計的な有意差ではない。

`extraAnswers` は基本回答の `ans` / `scores` と分離する。追加回答によって基本の4軸、タイプ、サブタイプを変えない。

この質問群は独自の振り返り用で、妥当性・信頼性は未検証。能力、健康状態、実際のストレス耐性、順位、採用適性は測定しない。50%は回答尺度の中間であり、集団平均ではない。画面にもこの限界を記載する。

## 保存と共有

- 途中保存は既存の `cq_p3` 内に `extraVersion: 1` と `extraAnswers` を追加する。古い保存データも読み込める。
- 結果表示時は既存仕様と同様に途中保存を消去する。結果表示後の再読み込みで追加結果を永続復元する機能は設けていない。
- 未回答、不正値、スキップ時に補足数値を作らない。
- 追加回答と数値を共有URLや解析イベントに含めない。タイプ紹介ページ・共有リンクからの閲覧者には補足数値を表示しない。

## 確認状況

- `make typecheck`: 通過。
- `make test`: 通過。従来の16タイプと200パターンのgolden回帰に加え、独立採点、保存・復帰、スキップ、境界値など20件の補足指標アサーションを追加。
- `make types`: 共有用の16ページを再生成。
- ブラウザでの見た目・操作確認、`make e2e`、`make lint` は今回未実行。既存のブラウザ検証には旧デザインの寸法・装飾・20問直後の結果遷移を固定した検査があるため、新しい体験に合わせた見直しと実機確認が必要。
- 公開mainへ自動反映せず、確認用ブランチとして渡す。マージ前にスマートフォン幅320/390px、デスクトップ、文字拡大、キーボード、追加回答あり/スキップの両経路を確認する。
